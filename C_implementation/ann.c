/**
 *train - Train an Artificial Neural Network
 *@data: matrix of data where rows are samples and columns are features
 *@label: matrix of labels where rows are samples and there is one column
 *@layer_size: number of neurons in the layer
 *Return: array of weights trained for the hidden and ouput layer
 */

